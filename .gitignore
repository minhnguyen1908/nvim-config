#Ignore build artifacts and temporary files
.DS_Store # macOS specific thumbnail/metadata files

# Neovim swap files
**/*.swp
**/*.swo
**/*.swn

# Backup files
**/*.bak
**/*.orig
**/*.tmp
**/*~

# Auto-generated help tags
doc/tags

# Plugin managers (e.g., Packer, Lazy.nvim, Plug)
# These directories contain downloaded plugins and should NOT be versioned.
# Instead, your init.lua or plugin file will specify *which* plugins to install.
# Adjust paths based on your plugin manager's setup
# For Lazy.nvim:
lazy/
# For Packer.nvim:
pack/packer/start/
# For Plug:
plugged/

# LSP (Language Server Protocol) related files
# Often stored in ~/.local/share/nvim/lsp_servers/ or similar,
# but some language servers might cache things in the config dir
.lsp/
.luarc.json # If generated by a Lua LSP server

# Compiled Lua files (often in `lua/` or `after/ftplugin/`)
**/*.luac

# Anything else you want to ignore
# env file
.env
